name: envanterimservetim
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.4
  font_awesome_flutter: ^10.1.0
  #bottom bar
  curved_navigation_bar: ^1.0.3
  fl_chart: ^0.63.0
  #barcodes
  flutter_barcode_scanner: ^2.0.0
  syncfusion_flutter_barcodes: ^23.1.39
  #http
  dio: ^5.3.2
  #
  shared_preferences: ^2.2.0
  image_picker: ^1.0.5
  #security
  local_auth: ^2.1.7
  #google_mobile_ads: ^4.0.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/welcome/
